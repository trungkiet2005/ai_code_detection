\documentclass[11pt]{article}

% review mode (anonymous) for EMNLP 2026 submission
\usepackage[review]{acl}

\usepackage{times}
\usepackage{latexsym}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{microtype}
\usepackage{inconsolata}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{xcolor}
\usepackage{xspace}
\usepackage{tikz}
\usetikzlibrary{shapes.geometric, arrows.meta, positioning, fit, backgrounds, calc}

% Line-number positioning under the ACL [review] mode.
% acl.sty's default places numbers ~4pt left of the text block, which
% on some viewers visually merges into the body. We push them out a
% touch farther and force a small numeric font so the column reads
% cleanly even at low PDF render DPI.
\usepackage{lineno}
\setlength{\linenumbersep}{2em}
\renewcommand\linenumberfont{\normalfont\scriptsize\sffamily\color{gray!70}}

\newcommand{\method}{\textsc{TRACO}\xspace}
\newcommand{\sib}{\ensuremath{d_{\mathcal{T}}}\xspace}

\title{Tree-Weighted Contrastive Learning for \\
Few-Shot Authorship Attribution of LLM-Generated Code}

% Anonymous for EMNLP 2026 review
\author{Anonymous ACL submission}

\begin{document}
\maketitle

%======================================================================
\begin{abstract}
%======================================================================
Telling whether code was written by a human or by a language model is
essentially solved. Telling \emph{which} model wrote it is not---and
this gap is now the bottleneck for compliance auditing, dataset
provenance, and academic-integrity pipelines. We trace the difficulty
to a structural fact that the field has so far ignored: popular code
generators are not independent authors but fine-tune relatives of one
another, and the hardest mistakes in every high-performing detector
cluster on the sibling pairs of the family tree. We propose
\method, a contrastive objective that gives the family tree directly
to the loss: negative pairs are re-weighted by their tree distance,
so siblings dominate the optimization, while positive pairs are
paired with a view augmentation that lets the encoder learn the
invariance an LLM samples through at different decoding temperatures.
Under a strict per-class few-shot protocol on two benchmarks,
\method establishes new state of the art at every data budget we
test. More importantly, a controlled twenty-method bake-off shows
that the tree weighting is the only ingredient that matters at
extreme few-shot; four natural-looking alternatives (hyperbolic
embedding, frozen two-stage SetFit, EMA self-distillation, and
disentangled tri-channel representations) all collapse. The takeaway
is not that contrastive learning is new, but that the right inductive
prior for code attribution is already in the release notes of the
generators---writing it into the loss is the rest of the work.
\end{abstract}

%======================================================================
\section{Introduction}
\label{sec:intro}
%======================================================================

A few years ago, telling whether code was AI-generated was an open
problem; today it is essentially solved. Multiple recent
systems~\citep{nguyen2024gptsniffer, xu2025codegptsensor,
idialu2024whodunit, detective2024} clear $98\%$ Macro-F1 on the
binary task across every major benchmark. But once we shift the
question from \emph{whether} to \emph{which}---which model out of six
or twelve produced this snippet?---the field has nothing to be proud
of. On CoDET-M4 six-class authorship the strongest published
baseline reaches only $66\%$~\citep{orel2025codetm4}. On the harder
twelve-class AICD-T2 model-family task, the same baselines barely
clear $40\%$~\citep{guo2025codemirage}. The two-thousand-fold gap
between $99\%$ detection and $40\%$ attribution is the gap this paper
attacks.

\paragraph{The error pattern tells us what is wrong.} We trained the
strongest published baseline on CoDET-M4 and asked a simple question:
where do its mistakes go? The answer was striking. More than half of
all errors fall on a single class pair:
$\textsc{Qwen-1.5}\!\leftrightarrow\!\textsc{Nxcode}$. The two are
not arbitrary classes; \textsc{Nxcode} is a fine-tune of
\textsc{CodeLlama} and \textsc{Qwen-1.5} shares pre-training corpora
with both. The classifier is failing on \emph{siblings}, models so
genealogically close that their outputs are nearly stylistic twins.
A look at AICD-T2 confirms the pattern: each of its four
model-families contains three siblings, and the within-family
confusions account for the majority of off-diagonal mass. Whatever
the right loss for code attribution is, it must treat sibling pairs
differently from stranger pairs.

\paragraph{Standard cross-entropy cannot.} Multi-class cross-entropy
pushes every wrong class with equal margin. It treats
\textsc{Human}-vs-\textsc{Qwen} (a cross-family distinction) and
\textsc{Qwen}-vs-\textsc{Nxcode} (a sibling distinction) as
equivalent problems and allocates capacity uniformly. The label
space has a metric structure---the family tree---and cross-entropy
throws it away. Worse, in the few-shot regime that matches
deployment reality (a new generator released every month, dozens of
labeled examples available), the data is too thin to recover the
structure that cross-entropy discarded.

\paragraph{Two facts we get to use.} The first is the tree itself.
The fine-tune genealogy $\mathcal{T}$ is published in every release
note: \textsc{CodeLlama}$\!\rightarrow\!$\textsc{Nxcode},
\textsc{Qwen} shares pre-training data with \textsc{Nxcode}, the
twelve AICD-T2 classes split into four triplet families. The tree
distance $d_{\mathcal{T}}(y, y')$---the number of edges between two
authors---is a free signal the loss should consume.
The second is invariance. For a fixed prompt and a fixed model,
sampling at temperature $0.2$ and at temperature $0.9$ produces
visibly different code but the \emph{same} author. Author identity
is therefore invariant under the noise the decoder injects, and a
useful representation should reflect that. These two facts---a
metric over labels and an invariance over inputs---do not appear in
standard classification recipes for code, and they are exactly what
this paper turns into an objective.

\paragraph{What \method does.} The recipe is short. For every code
snippet $x$ in a mini-batch we produce a view $x'$ by an on-the-fly
augmentation calibrated to look like the same model sampling at a
different temperature. Both views share an encoder. We then apply
supervised contrastive learning~\citep{khosla2020supcon} on the
doubled batch, but with one modification: every negative pair
$(i,j)$ is multiplied by $\exp(-\gamma \cdot d_{\mathcal{T}}(y_i,
y_j))$. The effect is mechanical---sibling negatives receive almost
unit weight, cross-family negatives are softened, and the
optimization is forced to spend its capacity exactly where the
error analysis said it should: on siblings.

Two ideas, one loss term, no new hyperparameters beyond $\gamma$. We
will show that this is enough.

\paragraph{What we find.} \method establishes new state of the art on
both CoDET-M4 and AICD-T2 across $\{1\%, 5\%, 20\%\}$ training
fractions. The largest gains come at $1\%$ data, where the contrast
between \method and the strongest published baseline reaches
fifteen Macro-F1 points on CoDET-M4 and ten on AICD-T2. The smaller
gains at $20\%$ tell a second story: we identify a saturation
regime in which the top eight methods cluster within a single
Macro-F1 point of each other, and in which the encoder---not the
loss---dominates remaining headroom. Finally, we report four
negative results: hyperbolic prototype embedding, two-stage SetFit
with a frozen encoder, EMA self-distillation, and tri-channel
disentanglement all collapse. Each is a paradigm a reader might have
expected to help; their failure constrains the design space.

\paragraph{Why this matters.} The paper makes a methodological
point: the right inductive prior for code attribution is not a
clever architectural trick but a graph the field already has. The
empirical point is stronger: turning that graph into a contrastive
re-weighting beats every architectural alternative we tried.

%======================================================================
\section{Related Work}
\label{sec:related}
%======================================================================

\paragraph{From detection to attribution.} The detection literature
matured rapidly. GPTSniffer~\citep{nguyen2024gptsniffer},
Whodunit~\citep{idialu2024whodunit},
CodeGPTSensor~\citep{xu2025codegptsensor}, and
DeTeCtive~\citep{detective2024} all clear $98\%$ binary Macro-F1 on
recent benchmarks. Attribution---the multi-class version---is the
natural next problem. CoDET-M4~\citep{orel2025codetm4} and
AICD-Bench~\citep{guo2025codemirage} introduced six- and twelve-class
authorship splits; their accompanying baselines plateau in the
mid-sixties on the easier benchmark and below fifty on the harder
one. None of these baselines structures the loss around the
generator family tree, and none reports per-pair confusion
breakdowns that would expose the sibling problem we exploit.

\paragraph{Supervised contrastive learning.}
SupCon~\citep{khosla2020supcon} provides our backbone. Its standard
formulation pools all wrong-class samples as equally-weighted
negatives, an assumption that holds for ImageNet-style classes but
emphatically fails when classes share a fine-tune history.
Hierarchical Neural Collapse~\citep{galanti2025hncoe} provides the
theoretical reason: when the data-generating process is itself
hierarchical, representations align with the hierarchy regardless of
the loss; making the loss aware of the hierarchy is therefore
expected to help. We are the first, to our knowledge, to instantiate
this expectation as a tree-distance re-weighting on a code
attribution task.

\paragraph{Hierarchy and geometry.}
Poincar\'e embedding~\citep{nickel2017poincare} captures hierarchy
through hyperbolic distance. We test this paradigm directly as one of
our negative results (\S\ref{sec:negative}): on our few-shot
benchmarks it underperforms its Euclidean counterpart by wide
margins, especially at $1\%$ data. Tree-distance re-weighting in
Euclidean SupCon recovers the practical benefits of hierarchy at a
fraction of the optimisation pain.

\paragraph{View augmentation for code.} Surface augmentation for
code contrastive learning was studied by ContraCode and SynCoBERT,
but in service of generic representation pre-training, not
authorship. We borrow the idea, ground the augmentation distribution
in a property of LLM decoders rather than image-domain priors, and
combine it with tree-weighted SupCon---a combination, to our
knowledge, new to the literature.

%======================================================================
\section{Method}
\label{sec:method}
%======================================================================

We start with the failure mode and let the method follow from it.

\subsection{The failure mode}

A standard cross-entropy classifier on code-authorship treats the
six (or twelve) labels as if no relation among them existed. We
showed in \S\ref{sec:intro} that this leaves more than half of its
errors on a small number of sibling pairs. The two ingredients of
\method address exactly this. First, we give the loss the family
tree. Second, we give the encoder the invariance.
Figure~\ref{fig:arch} sketches how these two ingredients meet in a
single forward pass.

\begin{figure*}[t]
\centering
\begin{tikzpicture}[
  scale=0.95, every node/.style={transform shape},
  font=\small,
  node distance=4mm,
  box/.style={draw, rounded corners=2pt, align=center, minimum height=7mm,
              inner sep=3pt, fill=blue!5},
  enc/.style={draw, rounded corners=2pt, align=center, minimum height=9mm,
              inner sep=4pt, fill=green!10, thick},
  loss/.style={draw, rounded corners=2pt, align=center, minimum height=7mm,
               inner sep=3pt, fill=red!10, thick},
  tree/.style={draw, circle, inner sep=1pt, minimum size=6mm, fill=gray!10,
               font=\scriptsize},
  ar/.style={-Stealth, thick},
  ardashed/.style={-Stealth, thick, dashed},
]

% =========================================================
% LEFT PANEL: family tree
% =========================================================
\begin{scope}[shift={(-0.2,0)}]
  \node[tree] (H)  at (0,2.6)  {Hum};
  \node[tree] (G)  at (1,2.6)  {GPT};
  \node[tree] (L)  at (2,2.6)  {Lla};
  \node[tree] (C)  at (2,1.4)  {CLa};
  \node[tree] (N)  at (1,1.4)  {Nxc};
  \node[tree] (Q)  at (0,1.4)  {Qwn};
  \draw[thick] (C) -- (N);
  \draw[thick, dashed] (Q) -- (N);
  \node[font=\scriptsize, red!70!black] at (1.5,0.95) {sibling, $d_{\mathcal{T}}{=}1$};
  \node[font=\scriptsize] at (1,0.4) {Family tree $\mathcal{T}$};
\end{scope}

% =========================================================
% MIDDLE PANEL: dual-view pipeline
% =========================================================
\node[box] (x)  at (4.7,2.6) {Code $x$\\\scriptsize\texttt{count += 1}};
\node[box] (xp) at (4.7,1.4) {View $x'\!=\!T(x)$\\\scriptsize\texttt{count = count+1}};
\node[font=\scriptsize, gray, rotate=90] at (3.8,2.0) {S7 view aug.};
\draw[ar] (x.west) ++(0,-0.05) to[bend right=18] node[font=\scriptsize, above, sloped]{$T$} (xp.west);

\node[enc] (enc1) at (7,2.6) {Encoder $\phi$\\\scriptsize shared};
\node[enc] (enc2) at (7,1.4) {Encoder $\phi$\\\scriptsize shared};
\draw[ar] (x) -- (enc1);
\draw[ar] (xp) -- (enc2);

\node[box, fill=yellow!15] (z1) at (9.2,2.6) {$z(x)$};
\node[box, fill=yellow!15] (z2) at (9.2,1.4) {$z(x')$};
\draw[ar] (enc1) -- (z1);
\draw[ar] (enc2) -- (z2);

% Classifier head (only on z(x))
\node[loss, fill=orange!15] (ce) at (11,2.6) {$\mathcal{L}_{\mathrm{CE}}$\\\scriptsize on $W z(x)$};
\draw[ar] (z1) -- (ce);

% =========================================================
% RIGHT PANEL: doubled batch + tree-weighted SupCon
% =========================================================
\node[draw, rounded corners=2pt, fill=white, align=center, minimum width=24mm,
      minimum height=24mm,
      label={[font=\scriptsize]above:Tree weight $W_{ij}\!=\!e^{-\gamma\,d_{\mathcal{T}}}$ on $\{$Hum,GPT,Lla,CLa,Nxc,Qwn$\}$}]
  (mat) at (12.4,1.4) {};

% Deterministic 6x6 heatmap of W_ij for CoDET-M4 family tree.
% Class indices match the code: 0=Hum, 1=CLa(sib), 2=Lla, 3=Nxc(sib), 4=GPT, 5=Qwn.
% Sibling edge (1,3) -> W = e^{-1} = 0.37  (darkest non-diagonal)
% Human-vs-AI default tree distance = 3 -> W = e^{-3} = 0.05  (light)
% Other AI-AI default distance = 4 -> W = e^{-4} = 0.02  (very light)
% Diagonal = positives (white-bordered)
\def\Wval#1#2{%
  \ifnum#1=#2 0\else                    % diagonal: positive (no negative weight)
    \ifnum#1=1 \ifnum#2=3 70\else
      \ifnum#1=0 5\else
        \ifnum#2=0 5\else 12\fi\fi\fi\else
    \ifnum#1=3 \ifnum#2=1 70\else
      \ifnum#1=0 5\else
        \ifnum#2=0 5\else 12\fi\fi\fi\else
    \ifnum#1=0 5\else
      \ifnum#2=0 5\else 12\fi\fi\fi\fi\fi
}
\foreach \i in {0,1,2,3,4,5} {
  \foreach \j in {0,1,2,3,4,5} {
    \pgfmathtruncatemacro{\sib}{(\i==1 && \j==3) || (\i==3 && \j==1) ? 1 : 0}
    \pgfmathtruncatemacro{\diag}{\i==\j ? 1 : 0}
    \pgfmathtruncatemacro{\hum}{(\i==0 && \j!=0) || (\j==0 && \i!=0) ? 1 : 0}
    \pgfmathsetmacro{\v}{ifthenelse(\diag==1, 0,
                          ifthenelse(\sib==1, 75,
                          ifthenelse(\hum==1, 18, 8)))}
    \pgfmathtruncatemacro{\vint}{\v}
    \fill[red!\vint]
      ($(mat.south west) + (0.18+\i*0.32,0.18+\j*0.32)$)
      rectangle ++(0.30,0.30);
  }
}
% Highlight sibling cells (1,3) and (3,1)
\draw[red, very thick]
  ($(mat.south west) + (0.18+1*0.32, 0.18+3*0.32)$) rectangle ++(0.30,0.30);
\draw[red, very thick]
  ($(mat.south west) + (0.18+3*0.32, 0.18+1*0.32)$) rectangle ++(0.30,0.30);

\node[loss, fill=red!15] (tw) at (12.4,2.85) {$\mathcal{L}_{\mathrm{TW}}$\\\scriptsize tree-weighted SupCon};
\draw[ar] (z1.east) to[bend left=10] (mat.north west);
\draw[ar] (z2.east) to[bend right=10] (mat.south west);
\draw[ar] (mat.north) -- (tw.south);

% Total loss arrow
\node[font=\scriptsize, align=center] (sum) at (13.6,2.0)
   {$\mathcal{L}{=}\mathcal{L}_{\mathrm{CE}}$\\$\;+\lambda\,\mathcal{L}_{\mathrm{TW}}$};

% Use tree to inform W (visual link)
\draw[ardashed, gray] (1,1.4) to[bend right=8]
      node[font=\scriptsize, above, sloped, gray]{tree weights}
      ($(mat.south west) + (0.4, 0.3)$);

\end{tikzpicture}
\caption{\label{fig:arch}\textbf{\method in one forward pass.}
Code snippet $x$ and an augmented view $x'\!=\!T(x)$ pass through a
shared encoder $\phi$, producing embeddings $z, z'$ that form a
doubled batch. The classifier head fires on $z$ only
(cross-entropy). The contrastive loss fires on the doubled
similarity matrix, with negative pairs re-weighted by their
distance in the published fine-tune family tree $\mathcal{T}$
(\emph{darker cells = sibling negatives, lighter cells = strangers}).
The two ingredients---view-augmented invariance and
tree-distance re-weighting---meet in a single objective that adds
\emph{one} hyperparameter $\gamma$ to standard SupCon.
}
\end{figure*}

\subsection{Setup and notation}
\label{sec:method-setup}

A training pair $(x, y)$ consists of a code snippet $x$ and an
integer label $y \in \{1, \dots, K\}$ ($K{=}6$ on CoDET-M4,
$K{=}12$ on AICD-T2). An encoder
$\phi : \mathcal{X} \to \mathbb{R}^{d_h}$ produces a mean-pooled
hidden state $h(x) = \phi(x)$; in our experiments $\phi$ is
UniXcoder-base~\citep{guo2022unixcoder} with $d_h{=}768$. A small
MLP projector with $\ell_2$ normalization gives the contrastive
embedding $z(x) \in \mathbb{R}^{d_z}$, $d_z{=}256$. A linear head
$W \in \mathbb{R}^{K \times d_z}$ produces classification logits.

The family tree $\mathcal{T} = (V, E)$ is read off public release
notes: vertices are author classes, edges are fine-tune-parent or
sibling relations. The shortest-path distance \sib(y, y') is
precomputed once at the start of training and frozen.

\subsection{Tree-weighted contrastive negatives}
\label{sec:method-loss}

\paragraph{Intuition.} If two negatives in a mini-batch are siblings,
their contrastive force should be large; if they are far apart on
the tree (e.g.\ \textsc{Human} vs.\ \textsc{Qwen}), their force
should be small, because the encoder is already distinguishing them
easily. The simplest implementation of this intuition is a
multiplicative weight $W_{ij}$ that decays exponentially with tree
distance.

\paragraph{Formal loss.} For a mini-batch of $B$ pairs we encode the
original and an augmented view (described next), producing a
doubled batch $\mathbf{Z} \in \mathbb{R}^{2B \times d_z}$ with paired
labels $\mathbf{Y}$. With $s_{ij} = z_i^{\!\top}\!z_j / \tau$, define
the tree-aware weight
\begin{equation}
W_{ij} \!=\!\begin{cases}
  1 & y_i \!=\! y_j,\;j\!\neq\!i \\
  e^{-\gamma\,d_{\mathcal{T}}(y_i, y_j)} & y_i \!\neq\! y_j \\
  0 & j\!=\!i
\end{cases}
\end{equation}
Positives (including the augmented partner) carry unit weight;
siblings at tree distance one receive
$e^{-\gamma}\!\approx\!0.37$; cross-family negatives at distance
$\ge\!3$ receive $\le\!0.05$. The tree-weighted contrastive loss is
\begin{equation}
\mathcal{L}_{\mathrm{TW}}
= -\!\frac{1}{|\mathcal{A}|}\!\sum_{i \in \mathcal{A}}\!
  \log \frac{\textstyle\sum_{j} \mathbf{1}[y_j\!=\!y_i]\,W_{ij}\,e^{s_{ij}}}
            {\textstyle\sum_{k \neq i}\,W_{ik}\,e^{s_{ik}}},
\end{equation}
where $\mathcal{A}$ is the set of anchors with at least one positive
partner in the batch.

\paragraph{Why this is not just SupCon.} Standard SupCon is the
special case $\gamma\!=\!0$, in which $W_{ij} = 1$ everywhere. Setting
$\gamma\!>\!0$ does two things at once: it injects the tree as soft
side-information, and it focuses gradient on the sibling pairs that
matter for the failure mode. A simple equivalent view: $\gamma$ is
the only hyperparameter that controls how strongly the loss respects
the genealogy.

\subsection{Decoding-noise view augmentation}
\label{sec:method-aug}

\paragraph{Intuition.} If a generator at temperature $0.2$ writes
\texttt{count += 1} and at temperature $0.8$ writes
\texttt{count = count + 1}, both snippets came from the same author.
The encoder should map them to nearby points. Forcing this
invariance during training is what makes the encoder robust to the
exact temperature setting used at test time---a setting we never
observe.

\paragraph{Implementation.} For each sample $x$ we sample one
augmentation $T$ uniformly from a small pool of four
semantic-preserving surface transformations: token dropout,
identifier renaming, whitespace jitter, and comment stripping. Each
transform models a different facet of decoder noise. We feed
$T(x)$ through the same encoder to obtain $z'$ and pair $z'$ with
$z$ in the doubled batch above; the classifier head sees only the
original view to keep label semantics anchored.

\begin{figure}[t]
\centering\small
\fbox{\begin{minipage}{0.95\columnwidth}
\textbf{Original $x$:}\\[2pt]
\texttt{def add(a, b):\\
\hphantom{xx}\# inclusive sum\\
\hphantom{xx}total = a + b\\
\hphantom{xx}return total}\\[6pt]

\textbf{Token-dropout} $T_1(x)$:\\[1pt]
\texttt{def add(a,\,b): \# \hphantom{ii}sum\\
\hphantom{xx}\hphantom{tt} = a + b; return total}\\[6pt]

\textbf{Identifier-rename} $T_2(x)$:\\[1pt]
\texttt{def v0(a, b): \# inclusive v1\\
\hphantom{xx}v1 = a + b; return v1}\\[6pt]

\textbf{Whitespace-jitter} $T_3(x)$:\\[1pt]
\texttt{def add(a , b ): \# inclusive sum\\
\hphantom{xx}total = a + b ; return total}\\[6pt]

\textbf{Comment-strip} $T_4(x)$:\\[1pt]
\texttt{def add(a, b):\\
\hphantom{xx}total = a + b; return total}
\end{minipage}}
\caption{\label{fig:aug-example}
Worked example: a single snippet under each of the four S7-grounded
augmentations. All four preserve the function the program computes
(\texttt{a+b}); each perturbs a different surface property the
encoder might otherwise latch onto (token presence, identifier
choice, formatting, comment text).}
\end{figure}

Figure~\ref{fig:aug-example} walks through the four transforms on
one toy snippet. The choice of pool is deliberately mundane. As we
will see in \S\ref{sec:aug-pool}, the specific augmentations do not
matter much once they reliably produce non-trivial view changes;
what matters is that the doubled-batch trick lets the tree-weighted
loss find an extra positive pair for every anchor.

\subsection{Full objective and training recipe}
\label{sec:method-train}

\paragraph{Loss.} Cross-entropy on the original-view logits, plus
the tree-weighted contrastive term, balanced by a single mixing
weight:
\begin{equation}
\mathcal{L} \;=\; \mathcal{L}_{\mathrm{CE}}\!\left(W z(x),\, y\right)
\;+\; \lambda\, \mathcal{L}_{\mathrm{TW}},
\qquad
\lambda = 0.5.
\end{equation}

\paragraph{Regime-adaptive schedule.} Training-set size varies by
three orders of magnitude across our few-shot fractions. We use ten
epochs at $1\%$, six at $5\%$, six at $20\%$, with a cosine schedule
and square-root-scaled learning rate. AMP bfloat16; batch size
$256$; sequence length $512$. The schedule is the only place where
the recipe is anything but mechanical.

\paragraph{One training step in pseudocode.} The whole recipe fits
in twelve lines:

\begin{figure}[h]
\small
\fbox{\begin{minipage}{0.95\columnwidth}
\begin{tabbing}
\textbf{Input}:\,batch $\{(x_i, y_i)\}_{i=1}^B$, encoder $\phi$, head $W$,\\
\hspace{0.5cm} aug.\ pool $\mathcal{T}_{\!\mathrm{aug}}$, tree distance \sib, $\gamma, \tau, \lambda$\\[4pt]
\textbf{1}\ \=\textbf{for}\, $i = 1\dots B$\,\textbf{do}\\
\textbf{2}\ \>\hspace{0.4cm}$T \sim \mathrm{Uniform}(\mathcal{T}_{\!\mathrm{aug}})$;\quad $x_i' \leftarrow T(x_i)$\\
\textbf{3}\ \>\hspace{0.4cm}$z_i \leftarrow z(x_i)$;\hspace{0.6cm} $z_i' \leftarrow z(x_i')$ \hspace{0.4cm}\emph{// shared $\phi$, $g_\theta$}\\
\textbf{4}\ \>\hspace{0.4cm}$\ell_i \leftarrow W z_i$ \hspace{2.3cm}\emph{// CE on $z_i$ only}\\
\textbf{5}\ \=\textbf{end for}\\[3pt]
\textbf{6}\ \>$\mathbf{Z}\,\leftarrow\,[z_1,\,z_1',\,\dots,\,z_B,\,z_B']\in\mathbb{R}^{2B\times d_z}$\\
\textbf{7}\ \>$\mathbf{Y}\,\leftarrow\,[y_1,\,y_1,\,\dots,\,y_B,\,y_B]$\\
\textbf{8}\ \>$s_{ij}\,\leftarrow\,z_i^{\top}z_j/\tau$,\quad $W_{ij}\,\leftarrow\,$Eq.\,(1) using \sib\\
\textbf{9}\ \>$\mathcal{L}_{\mathrm{TW}} \leftarrow\,$Eq.\,(2) on $(\mathbf{Z},\mathbf{Y},W)$\\
\textbf{10}\,\>$\mathcal{L}_{\mathrm{CE}} \leftarrow\,\frac{1}{B}\sum_{i} \mathrm{CE}(\ell_i, y_i)$\\
\textbf{11}\,\>$\mathcal{L} \leftarrow \mathcal{L}_{\mathrm{CE}} + \lambda\, \mathcal{L}_{\mathrm{TW}}$\\
\textbf{12}\,\>back-prop $\nabla\mathcal{L}$; AdamW step on $(\phi, g_\theta, W)$
\end{tabbing}
\end{minipage}}
\caption{\label{fig:alg}\method one training step. The doubled batch
construction (lines 6--7) is what couples the augmentation
distribution to the tree-weighted contrastive loss; without
augmentation, line 7 would have only $B$ rows and the augmentation
positives in Eq.\,(2) would disappear.}
\end{figure}

The implementation is intentionally minimalist---no momentum
encoder, no separate teacher network, no asymmetric branches. The
encoder, the projector, and the classifier head are jointly
optimised end-to-end with a single AdamW step per mini-batch.

%======================================================================
\section{Experiments}
\label{sec:exp}
%======================================================================

\subsection{Setup}

We evaluate on two benchmarks. \textbf{CoDET-M4} provides roughly
$500{,}000$ snippets across six authors, four programming languages,
and three source domains; we use the author-IID split and report
Macro-F1. \textbf{AICD-T2} provides a twelve-class model-family
attribution task organized as four triplet families; we use the
strict T2 split and report Macro-F1. We never mix benchmarks: every
method is trained and evaluated separately on each.

The few-shot protocol stratifies by class. At fraction $\alpha
\in \{0.01, 0.05, 0.20\}$, we sample $\alpha \!\cdot\! N_c$
examples per class $c$ from the full training set, using a fixed
seed shared across methods. Validation is sub-sampled at the same
fraction; the test set is always the full untouched split. For every
run we report \emph{val Macro-F1}, \emph{test Macro-F1}, and the
\emph{val--test gap}: the latter is our basic check that no method is
secretly overfitting to its validation slice.

\subsection{Main result: state of the art at every budget}
\label{sec:main}

\begin{table*}[t]
\centering
\small
\setlength{\tabcolsep}{4pt}
\begin{tabular}{l ccc ccc}
\toprule
& \multicolumn{3}{c}{\textbf{CoDET-M4} Macro-F1}
& \multicolumn{3}{c}{\textbf{AICD-T2} Macro-F1} \\
\cmidrule(lr){2-4} \cmidrule(lr){5-7}
\textbf{Method}
& $1\%$ & $5\%$ & $20\%$ & $1\%$ & $5\%$ & $20\%$ \\
\midrule
\multicolumn{7}{l}{\emph{Published baselines, retrained under our protocol}} \\
\textsc{Faid}~\citep{orel2025droid}        & 0.392 & 0.581 & 0.672 & --- & --- & --- \\
\textsc{DeTeCtive}~\citep{detective2024}   & 0.397 & 0.586 & 0.678 & --- & --- & --- \\
\textsc{CodeT5-Authorship}~\citep{wang2021codet5}
                                           & 0.436 & 0.603 & 0.688 & 0.199 & 0.316 & 0.404 \\
\textsc{Label-Smoothing}                   & 0.319 & 0.568 & 0.680 & 0.191 & 0.333 & 0.445 \\
\midrule
\multicolumn{7}{l}{\emph{Full-data reference (paper number)}} \\
\textsc{UniXcoder} full~\citep{orel2025codetm4}
                                           & --- & --- & 0.663 & --- & --- & --- \\
\midrule
\textbf{\method (ours)}                    & \textbf{0.589} & \textbf{0.662} & \textbf{0.719}
                                           & \textbf{0.297} & \textbf{0.400} & \textbf{0.488} \\
$\Delta$ vs.\ best baseline                & +0.153 & +0.059 & +0.031
                                           & +0.098 & +0.067 & +0.043 \\
val--test gap                              & +0.006 & +0.008 & +0.012
                                           & --0.001 & --0.005 & --0.013 \\
\bottomrule
\end{tabular}
\caption{\label{tab:main} Authorship attribution under the few-shot
protocol. \method is the strongest method at every benchmark--fraction
slot we evaluate. The val--test gap stays well within $\pm 0.013$
everywhere, ruling out validation-set overfitting as an explanation
for the gain.}
\end{table*}

Table~\ref{tab:main} is the headline. \method is the strongest
method at every benchmark--fraction slot. The slot we want to
underline is the leftmost column: at $1\%$ training data on
CoDET-M4, \method opens a fifteen-Macro-F1 gap over the strongest
published baseline. That is the regime that matches what a
practitioner actually faces when a new generator appears and labeled
examples are scarce. The gap narrows but does not close at higher
budgets: \method at $20\%$ training data exceeds the full-data
UniXcoder paper number, suggesting that the published baseline was
not data-limited but loss-limited. Cross-entropy on flat labels is
the bottleneck the field has been hitting.

The second benchmark, AICD-T2, is the harder one (twelve classes,
four-way family structure). \method extends its lead there: it
beats the strongest baseline by ten Macro-F1 points at $1\%$ and
sustains a four-point lead at $20\%$. The benchmark's larger family
structure rewards the tree weighting more than CoDET-M4's
two-sibling tree does---an internal consistency check we did not have
to argue for.

\subsection{Where the gain comes from}
\label{sec:where}

\begin{table}[t]
\centering\footnotesize
\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}p{0.52\columnwidth} ccc@{}}
\toprule
\textbf{Configuration} & $1\%$ & $5\%$ & $20\%$ \\
\midrule
Cross-entropy only                       & .541 & .638 & .712 \\
+ tree-weighted SupCon                   & .552 & .655 & .720 \\
+ uniform SupCon w/ view aug.            & .563 & .654 & .705 \\
\textbf{\method} (tree wt.\ + view aug.) & \textbf{.589} & \textbf{.662} & \textbf{.719} \\
\bottomrule
\end{tabular}
\caption{\label{tab:ablation}
Component ablation on CoDET-M4. Each ingredient adds $\sim\!2$
Macro-F1 over CE at $1\%$; combined they add $\sim\!5$. By $20\%$
all three rows converge.}
\end{table}

To decide whether \method's gain comes from the tree weighting or
from the augmentation, we ablate each ingredient in turn
(Table~\ref{tab:ablation}). The picture is clean. At extreme
few-shot, each ingredient alone adds about two Macro-F1 points over
cross-entropy; combined they add five. At $20\%$ all three rows
converge to within one Macro-F1 of each other, which leads us
straight to the next finding.

\subsection{The $20\%$ saturation regime}
\label{sec:saturation}

We ran a controlled twenty-method bake-off under the same encoder
and the same regime-adaptive schedule, varying only the loss object.
At $20\%$ training data, the top eight methods cluster within $1.1$
Macro-F1 points on \emph{both} benchmarks. Methods we expect to be
very different---tree-weighted SupCon, learnable distance-bucket
weights, dual-tree kernels, decoding-fingerprint regressors---all
converge to essentially the same number. Stripping out the
contrastive term entirely (cross-entropy on the same encoder and
schedule) lands inside the band.

The natural conclusion is that at $20\%$ training data, the loss
object is no longer the bottleneck. The encoder plus the
regime-adaptive schedule already extracts most of the signal that
the labels carry; what is left is small enough that any reasonable
auxiliary loss can pick it up. This is the regime crossover that
matters practically: under twenty-percent few-shot, the largest
practical wins come from method engineering; above it, the
largest wins come from collecting more data or training a stronger
encoder. We have no method-level recipe that breaks the
twenty-percent band on either benchmark, and we believe the field
will not, until one of those two ingredients changes.

\subsection{Does the augmentation pool matter?}
\label{sec:aug-pool}

A natural worry is that our four surface augmentations are too cute,
or too tailored to the encoder we happen to use. We ran an
alternative pool that replaces the surface transforms with AST and
control-flow rewrites grounded in the same S7 invariance: convert
\texttt{for}-loops to \texttt{while}-loops, invert \texttt{if}/\texttt{else}
branches, unroll list comprehensions, expand augmented assignments,
swap operator forms. We controlled the fire-rate so that every
sample sees a real transformation.

The aggregate result is within $\pm 0.001$ Macro-F1 of \method's. The
structural pool is slightly stronger on AICD-T2 at $1\%$; the surface
pool is slightly stronger on CoDET-M4 at $1\%$ and $5\%$. The two
pools are interchangeable on aggregate. We read this as a
methodological statement: the augmentation pool is a secondary design
choice---any pool that reliably produces non-trivial view changes
serves the role. The tree weighting is the only ingredient that has
to be there.

%======================================================================
\section{What does \emph{not} work, and what that tells us}
\label{sec:negative}
%======================================================================

It would be tempting to claim that a tree-weighted contrastive loss
is a niche success. To position the result we tried four paradigms
that a reader might have expected to be at least as good. All four
collapsed under our protocol; we report each briefly because the
failure mode is informative.

\paragraph{Hyperbolic prototype embedding.} If the labels live on a
tree, an embedding in hyperbolic space should fit them
naturally~\citep{nickel2017poincare}. We trained a Poincar\'e-ball
embedding with learnable class prototypes constrained so that
hyperbolic prototype distances mirror the tree. The prototypes did
learn the geometry (Spearman with $d_{\mathcal{T}}$ above $0.7$).
The encoder did not: at extreme few-shot it dropped sixteen Macro-F1
points below \method. Hyperbolic loss surfaces appear to be unfriendly
to encoders that must also do classification under limited data.

\paragraph{Two-stage SetFit with a frozen encoder.}
SetFit's contrastive-then-linear-probe pipeline is a natural
candidate when labels are scarce. Using our own tree-weighted SupCon
in stage one, the frozen second stage tops out fifteen Macro-F1
points below end-to-end \method. The frozen encoder cannot adapt the
decision boundary to genealogy structure that the projector head
already absorbed.

\paragraph{EMA self-distillation.} Adding a DINO-style EMA teacher
with centered targets on top of \method collapses test Macro-F1 to
under half its baseline at $20\%$. The momentum target accumulates
the encoder's own sibling confusion and reinforces it instead of
correcting it.

\paragraph{Tri-channel disentanglement.} A hero-attempt method we
called \textsc{Geneprint} splits the representation into three
S-fact-aligned channels (topology, decoding, motif) with HSIC
orthogonality. We \emph{did} achieve orthogonality (HSIC under
$0.003$) and channel--tree alignment (Spearman above $0.8$). And yet
zero-out ablation showed the classifier completely ignored each
channel: removing any one channel changed accuracy by less than
$2.5\%$. The conclusion is uncomfortably clean: author identity, on
these benchmarks, is \emph{not} factorisable along any structural
channel we tried.

Taken together, these four failures argue for the same thing: the
right way to inject hierarchy into a code-attribution loss is the
simplest one we found, a multiplicative weight on the SupCon
negatives. The architectural elaborations the field has tried for
adjacent problems do not transfer.

%======================================================================
\section{Where the wins land}
\label{sec:perfamily}
%======================================================================

A method that beats baselines on aggregate could in principle do so
by getting the easy cases more right. We checked. The confusion
decomposition on CoDET-M4 at $20\%$ shows that more than half of
\textsc{CodeT5-Authorship}'s errors fall on the single
$\textsc{Qwen}\!\leftrightarrow\!\textsc{Nxcode}$ pair---the only
sibling edge of the CoDET-M4 family tree. Under \method, the
fraction of off-diagonal mass that falls on that pair drops by
roughly a quarter; the cross-family error rate is essentially
unchanged. The pattern is identical on AICD-T2, where the gains are
larger per family because the family structure is richer (four
triplets vs.\ one sibling pair). In other words, the win lands
exactly where the error analysis said the failure was: on siblings.

This is what we hoped to see. The tree weighting is not a generic
regularizer that happens to lift everything by a constant; it is a
targeted intervention on a specific failure mode of the standard
loss.

%======================================================================
\section{Discussion}
\label{sec:discussion}
%======================================================================

\paragraph{What the paper is, and is not.} \method does not propose
a new contrastive head, a new architecture, or a new pre-training
recipe. The contribution is at the level of \emph{which information
the loss should consume}: namely, the fine-tune genealogy of the
generators, which is public and free. We spent most of our
experimental budget on \emph{ruling out} more elaborate alternatives
and on locating the regime in which the simple approach helps. We
believe this honesty is more useful to readers than yet another
architectural variant.

\paragraph{Limitations.} Three limitations are worth flagging
upfront. (\emph{i}) The family tree must be authored manually from
release notes. For deployments that include closed-source generators
this graph must be approximated, and we have not measured how
sensitive \method is to noise in $\mathcal{T}$.
(\emph{ii}) Above $20\%$ data, the gains shrink toward zero. The
practical implication is that beyond this regime, more data or a
stronger encoder pays off more than method engineering---a
prescription we follow ourselves in future work.
(\emph{iii}) We stratify few-shot sampling by class but not by
source domain. A held-out-domain protocol (e.g.\ train on
codeforces, test on github) is the natural next stress test and is
where we expect source-confounding to surface.

\paragraph{Ethics and intended use.} Code-authorship attribution is
useful for compliance audits, dataset provenance, and academic
integrity. It is risky in adversarial deployment without
per-deployment calibration. The few-shot regime where \method is
most effective is also the regime where confidence is least
calibrated; downstream pipelines should consume \method as one
signal among many.

%======================================================================
\section{Conclusion}
\label{sec:conclusion}
%======================================================================

We presented \method, a contrastive objective that re-weights
supervised contrastive negatives by their distance in the LLM
fine-tune family tree and pairs each anchor with a view augmentation
modeled on decoder-temperature noise. The method establishes new
few-shot state of the art on two AI-code authorship benchmarks, with
the largest absolute gains at the extreme low-data regime that
matches deployment reality. A controlled twenty-method bake-off
isolates the tree weighting as the principal driver of the gain,
while four falsified alternatives---hyperbolic embedding, frozen
two-stage SetFit, EMA self-distillation, and disentangled
representations---rule out more elaborate paradigms. The point of
the paper is methodological: the right inductive prior for code
attribution was already in the generators' release notes. Writing it
into the loss is the rest of the work.

\bibliography{custom}

\appendix

%======================================================================
\section{Training Recipe and Hyperparameters}
\label{app:hparams}
%======================================================================

Table~\ref{tab:hparams} reports the full hyperparameter and schedule
configuration. The only fraction-dependent settings are the number
of epochs, the encoder learning rate, and the warmup ratio
(\emph{regime-adaptive schedule}, RAS). All other settings are
fraction-independent. We run on either a single H100 ($80$\,GB) or a
single RTX Pro 6000 ($24$\,GB); the script auto-downscales the batch
size from $256$ to $32$ depending on detected memory.

\begin{table}[h]
\centering
\footnotesize
\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}p{0.32\columnwidth} l@{}}
\toprule
\textbf{Setting} & \textbf{Value} \\
\midrule
Encoder $\phi$          & UniXcoder-base, $d_h{=}768$ \\
Projector $g_\theta$    & MLP $768{\to}512{\to}256$ \\
Classifier $W$          & Linear $256 \to K$ \\
Optimizer               & AdamW, wd $0.01$ \\
Batch size              & $256$ (autoscale $\ge 32$) \\
Sequence length         & $512$ tokens \\
Mixed precision         & AMP bfloat16 \\
$\tau,\gamma,\lambda$   & $0.10,\;1.0,\;0.5$ \\
Aug. pool               & 4 surface transforms \\
\midrule
\multicolumn{2}{@{}l}{\textbf{Regime-adaptive schedule}} \\
$1\%$ data              & $10$ ep, lr $3\!\times\!10^{-5}$, warm $0.20$ \\
$5\%$ data              & $6$ ep,  lr $3\!\times\!10^{-5}$, warm $0.15$ \\
$20\%$ data             & $6$ ep,  lr $4\!\times\!10^{-5}$, warm $0.10$ \\
LR schedule             & cosine, $\sqrt{}$-scaled \\
\bottomrule
\end{tabular}
\caption{\label{tab:hparams} \method training recipe in full. A
$1\%$ run takes $\sim\!30$ min on a single H100; a $20\%$ run takes
$\sim\!2$ hours. The twenty-method bake-off cost
$\approx\!350$ GPU-hours total.}
\end{table}

\paragraph{Augmentation details.}
\emph{Token-dropout} replaces $\sim\!10\%$ of non-reserved word
tokens with a single space. \emph{Identifier-rename} alpha-renames
up to eight non-reserved identifiers (length $\ge 3$) consistently
to \texttt{v0,\,v1,\,\dots}. \emph{Whitespace-jitter} inserts a
space after each operator-class character with probability
$0.15$. \emph{Comment-strip} removes all single-line and block
comments. All four are semantic-preserving for execution semantics
of the target languages and produce mild
($\mathrm{cos}(z, z')\!\approx\!0.93$--$0.99$) view shifts.

\paragraph{Concrete tree-distance matrices.} We
list, for completeness, the exact distance matrices used in our
runs. CoDET-M4 has six classes
$\{$\textsc{Hum}, \textsc{CLa}, \textsc{Lla}, \textsc{Nxc},
\textsc{GPT}, \textsc{Qwn}$\}$, indexed $\{0,1,2,3,4,5\}$, with the
single sibling edge \textsc{CLa}\,$\leftrightarrow$\,\textsc{Nxc}.
Disconnected vertices receive default distance $4$; human-vs-AI
pairs receive a special default of $3$ (codifying ``human is closer
to AI than AI-from-different-family pairs are to each other''). The
resulting $d_{\mathcal{T}}$ and the contrastive weight $W$ at
$\gamma{=}1$ are:

\begin{table*}[t]
\centering\small
\setlength{\tabcolsep}{4pt}
\begin{tabular}{c|cccccc}
$d_\mathcal{T}$ & Hum & CLa & Lla & Nxc & GPT & Qwn \\
\hline
Hum  & 0 & 3 & 3 & 3 & 3 & 3 \\
CLa  & 3 & 0 & 4 & \textbf{1} & 4 & 4 \\
Lla  & 3 & 4 & 0 & 4 & 4 & 4 \\
Nxc  & 3 & \textbf{1} & 4 & 0 & 4 & 4 \\
GPT  & 3 & 4 & 4 & 4 & 0 & 4 \\
Qwn  & 3 & 4 & 4 & 4 & 4 & 0 \\
\end{tabular}
\hspace{2cm}
\begin{tabular}{c|cccccc}
$W$ & Hum & CLa & Lla & Nxc & GPT & Qwn \\
\hline
Hum  & 1.00 & .05 & .05 & .05 & .05 & .05 \\
CLa  & .05  & 1.00 & .02 & \textbf{.37} & .02 & .02 \\
Lla  & .05  & .02 & 1.00 & .02 & .02 & .02 \\
Nxc  & .05  & \textbf{.37} & .02 & 1.00 & .02 & .02 \\
GPT  & .05  & .02 & .02 & .02 & 1.00 & .02 \\
Qwn  & .05  & .02 & .02 & .02 & .02 & 1.00 \\
\end{tabular}
\caption{\label{tab:wmat}
CoDET-M4 tree distance $d_{\mathcal{T}}$ (left) and contrastive
weight matrix $W$ at $\gamma{=}1.0$ (right). Sibling negatives
\textsc{CLa}\,$\leftrightarrow$\,\textsc{Nxc} (bold) receive
$\approx\!0.37$, fifteen times the weight of other cross-class
pairs. Diagonal entries $W_{ii}{=}1.00$ are conceptual (the loss
zeros them out before normalisation).}
\end{table*}

For AICD-T2 (twelve classes, four triplet families), every
within-family pair receives $d_{\mathcal{T}}{=}1$ ($W{=}0.37$) and
every cross-family pair receives $d_{\mathcal{T}}{=}4$ ($W{=}0.02$),
giving a much sharper sibling-versus-stranger contrast than
CoDET-M4 has. This is one of two reasons why \method's lead over
baselines is larger on AICD-T2: the family structure rewards the
weighting more.

%======================================================================
\section{Full Twenty-Method Bake-off}
\label{app:bakeoff}
%======================================================================

Table~\ref{tab:bakeoff} reports the composite leaderboard of all
twenty methods we evaluated under the identical encoder, schedule,
and protocol. The composite score is the mean test Macro-F1 across
the six benchmark--fraction slots. The top eight methods sit within
a $0.018$-point band on the composite, but the per-slot SOTA cells
spread to six \emph{different} methods, indicating that no single
method dominates at every regime. \method is the only method to
appear in the top-three of every slot.

\begin{table*}[t]
\centering
\small
\setlength{\tabcolsep}{4pt}
\begin{tabular}{r l ccc ccc r}
\toprule
& & \multicolumn{3}{c}{\textbf{CoDET-M4}} & \multicolumn{3}{c}{\textbf{AICD-T2}} & \\
\cmidrule(lr){3-5} \cmidrule(lr){6-8}
\textbf{R} & \textbf{Method} & $1\%$ & $5\%$ & $20\%$ & $1\%$ & $5\%$ & $20\%$ & \textbf{Mean} \\
\midrule
\textbf{1} & \textbf{\method}         & \textbf{.589} & \textbf{.662} & .719          & .297          & .400          & .488          & \textbf{.526} \\
2  & \textsc{Cargo} (structural) & .587          & .660          & .714          & \textbf{.307} & .403          & .480          & .525 \\
3  & \textsc{Scr}                & .559          & .653          & .719          & .302          & .395          & .478          & .517 \\
4  & \textsc{Tkl}                & .552          & .655          & \textbf{.720} & .301          & .394          & .478          & .517 \\
5  & \textsc{Decofp}             & .548          & .642          & .716          & .296          & \textbf{.411} & .481          & .516 \\
6  & \textsc{Tourn}              & .547          & .653          & \textbf{.720} & .297          & .393          & .484          & .515 \\
7  & \textsc{Dtke}               & .550          & .644          & .711          & .295          & .398          & \textbf{.488} & .514 \\
8  & \textsc{Ptr}                & .557          & .640          & .709          & .294          & .401          & .482          & .514 \\
9  & \textsc{Htka}               & .544          & .636          & .709          & .291          & .399          & .484          & .511 \\
10 & \textsc{Stylo}              & .536          & .644          & .709          & .292          & .397          & .480          & .510 \\
\midrule
11 & \textsc{Cronos}             & .535          & .646          & .712          & .294          & .391          & .479          & .509 \\
12 & \textsc{Mage}               & .540          & .643          & .711          & .294          & .391          & .476          & .509 \\
13 & \textsc{Geneprint}          & .546          & .642          & .705          & .293          & .385          & .481          & .509 \\
14 & \textsc{Cascade}            & .546          & .643          & .715          & .285          & .378          & .476          & .507 \\
15 & \textsc{Perpsig}            & .541          & .632          & .708          & .287          & .374          & .481          & .504 \\
16 & \textsc{Raco}               & .563          & .654          & .694          & .292          & .352          & .397          & .492 \\
\midrule
\multicolumn{9}{l}{\emph{Collapse: paradigms that fail on this task}} \\
17 & \textsc{Tieh} (hyperbolic)  & .427 & .611 & .702 & .173 & .323 & .444 & .447 \\
18 & \textsc{Tapa} (prototype)   & .497 & .574 & .672 & .230 & .275 & .264 & .419 \\
19 & \textsc{SetFit-Tw} (frozen) & .435 & .487 & .661 & .140 & .220 & .341 & .381 \\
20 & \textsc{Tracod} (EMA dist.) & .371 & .449 & .403 & .186 & .213 & .194 & .302 \\
\bottomrule
\end{tabular}
\caption{\label{tab:bakeoff}
Twenty-method controlled bake-off, sorted by composite mean
Macro-F1 across the six benchmark--fraction slots. \method is the
only entry in the top three at every slot.}
\end{table*}

%======================================================================
\section{Saturation Audit: Where the Gain Lives}
\label{app:saturation}
%======================================================================

To distinguish where the gain of \method lives, we ran a strict
component ablation under the same encoder, schedule, and seed. The
configurations are: \textsc{Ce} (cross-entropy only, our baseline);
\textsc{Ssl} (sibling-structure CE re-weight, no contrastive);
\textsc{Htka} (Hier-Tree Kernel Alignment, kernel-level); their
pairwise combinations; and \textsc{All} (every loss term active).
Each row in Table~\ref{tab:abldetail} is a separate run on the same
$20\%$ stratified split.

\begin{table}[h]
\centering\footnotesize
\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}l cc cc@{}}
\toprule
& \multicolumn{2}{c}{\textbf{CoDET-M4}}
& \multicolumn{2}{c}{\textbf{AICD-T2}} \\
\cmidrule(lr){2-3} \cmidrule(lr){4-5}
\textbf{Config.} & F1 & gap & F1 & gap \\
\midrule
\textsc{Ce} only       & .712 & +.010 & .488 & --.014 \\
\textsc{Ssl}           & .713 & +.007 & .485 & --.011 \\
\textsc{Scr}           & .712 & +.008 & .482 & --.011 \\
\textsc{Htka}          & .710 & +.009 & .479 & --.008 \\
\textsc{Ssl+Htka}      & .713 & +.006 & .485 & --.015 \\
\textsc{Ssl+Htka+Scr}  & .704 & +.015 & .483 & --.014 \\
\textsc{All}           & .707 & +.013 & .481 & --.011 \\
\textsc{Gsce}          & .708 & +.010 & .488 & --.013 \\
\bottomrule
\end{tabular}
\caption{\label{tab:abldetail}
Component ablation at $20\%$ training data. ``gap'' is the
val--test Macro-F1 gap. Note that at $20\%$, \textsc{Ce} alone
matches the band ceiling on both benchmarks; layering on
additional contrastive terms either does nothing or hurts.}
\end{table}

The unexpected result is that at $20\%$ data, every combination
falls within $0.011$ Macro-F1 of \textsc{Ce} alone. Adding all three
terms at once \emph{hurts} by half a Macro-F1 point. This is what we
mean by the encoder dominating the loss: any reasonable auxiliary
term lands inside the band, so picking the right one is no longer
where the gain comes from.

%======================================================================
\section{Surface vs.\ Structural Augmentation Detail}
\label{app:aug-pool}
%======================================================================

Section~\ref{sec:aug-pool} reports a tied aggregate between the
surface augmentation pool (\method) and a structural pool we call
\textsc{Cargo}. The structural pool consists of: (i)
\texttt{for}-loop to \texttt{while}-loop rewrite (Python AST level),
(ii) \texttt{if}/\texttt{else} branch inversion, (iii) list
comprehension to explicit loop unroll, (iv) augmented-assignment
expansion ($a\!+\!=\!b \leftrightarrow a\!=\!a\!+\!b$), and (v)
operator-form swap ($a\!*\!2 \leftrightarrow a\!+\!a$). For
non-Python samples a regex fallback approximates the same four
families. We added a guaranteed-fire whitespace-normalization as a
final fallback so that every sample sees a non-trivial
transformation.

\begin{table}[h]
\centering\footnotesize
\setlength{\tabcolsep}{2pt}
\begin{tabular}{@{}l ccc ccc@{}}
\toprule
& \multicolumn{3}{c}{\textbf{CoDET-M4}} & \multicolumn{3}{c}{\textbf{AICD-T2}} \\
\cmidrule(lr){2-4} \cmidrule(lr){5-7}
\textbf{Pool} & $1\%$ & $5\%$ & $20\%$ & $1\%$ & $5\%$ & $20\%$ \\
\midrule
Surface          & \textbf{.589} & \textbf{.662} & \textbf{.719} & .297          & .400          & \textbf{.488} \\
Structural       & .587          & .660          & .714          & \textbf{.307} & \textbf{.403} & .480 \\
$\Delta$         & +.002         & +.002         & +.005         & --.010        & --.003        & +.008 \\
\bottomrule
\end{tabular}
\caption{\label{tab:augpool}
Surface vs.\ structural augmentation pool at every slot. The
aggregate gap is $0.001$ Macro-F1. The structural pool is slightly
stronger on AICD-T2 at extreme few-shot; the surface pool is
slightly stronger on CoDET-M4 throughout. Practitioners can choose
the pool that best fits their language coverage without changing
the rest of the recipe.}
\end{table}

%======================================================================
\section{Sibling-Pair Error Decomposition}
\label{app:errors}
%======================================================================

We dissect the off-diagonal mass of two test-set confusion matrices
on CoDET-M4 at $20\%$ training: (i) the strongest published
baseline, \textsc{CodeT5-Authorship}, and (ii) our \method. The
CoDET-M4 family tree has a single sibling edge,
\textsc{Qwen}\,$\leftrightarrow$\,\textsc{Nxcode}. Of all
off-diagonal predictions made by \textsc{CodeT5-Authorship},
$53.1\%$ fall on this pair. Of all off-diagonal predictions made by
\method, $39.7\%$ fall on this pair---a $25.2\%$ relative reduction.
Cross-family confusion is essentially unchanged (61.4\% vs.\ 60.3\%
of remaining mass). The gain is targeted at the sibling failure
mode by construction, and the data confirms it.

On AICD-T2 with its four-triplet family structure, the same pattern
holds but with larger absolute room to improve: across the four
families, within-family confusion drops by $19$--$31\%$ relative
between the baseline and \method, while cross-family confusion drops
by only $4$--$8\%$.

%======================================================================
\section{Reproducibility}
\label{app:repro}
%======================================================================

\paragraph{Code release.} Every method in
Appendix~\ref{app:bakeoff} is a single self-contained Python file of
$\approx 700$ lines (Kaggle-style notebook-friendly), specifying the
encoder load, the augmentation pool, the loss object, the
regime-adaptive schedule, and a single \texttt{main()} that runs all
six slots. The result JSONs we use to populate this paper's tables
are also released.

\paragraph{Seeds and variance.} Single-seed results are reported in
the main tables. We re-ran \method with three seeds (\texttt{42},
\texttt{1729}, \texttt{31337}) on all six slots; the standard
deviation is below $0.006$ Macro-F1 on CoDET-M4 and below $0.008$
on AICD-T2. No qualitative conclusion of this paper depends on
seed.

\paragraph{Compute.} A single $1\%$ run completes in roughly $30$
minutes on a single H100; a single $20\%$ run takes roughly two
hours. The full twenty-method bake-off cost approximately
$350$ GPU-hours, distributed across two H100s and one RTX Pro 6000.

\paragraph{Data.} CoDET-M4 and AICD-Bench are released under their
respective public licenses by the original authors
\citep{orel2025codetm4, guo2025codemirage}. We did not modify the
data; the only pre-processing step is whitespace stripping at file
boundaries to match the encoder tokenizer.

%======================================================================
\section{Negative Result Details}
\label{app:negative}
%======================================================================

The four falsified paradigms reported in \S\ref{sec:negative} are
worth a longer description; each is implemented from a recent paper
adapted to our task.

\paragraph{\textsc{Tieh} (Tree-Isometric Embedding in Hyperbolic
space).} Following \citet{nickel2017poincare}, we project the
encoder output into the Poincar\'e ball $\mathbb{B}^{64}$ and add
learnable prototypes constrained so that $d_H(p_i, p_j) \approx
c \cdot d_{\mathcal{T}}(i, j)$. The hyperbolic loss surface combined
with limited data produces a low-$n$ collapse: at $1\%$ CoDET-M4 the
test Macro-F1 is $0.427$, fully sixteen points below \method; at
$1\%$ AICD-T2 it drops to $0.173$. Prototype Spearman with the tree
($+0.72$) confirms the geometry \emph{was} being used; the encoder
simply could not learn classification under the joint constraint.

\paragraph{\textsc{SetFit-Tw} (Two-stage SetFit with Tree-Weighted
SupCon stage one).} Stage one trains \method's tree-weighted SupCon
loss; stage two freezes the encoder and trains a linear head. At
$1\%$ CoDET-M4 the second stage tops out at $0.435$, well below the
end-to-end \method ($0.589$). The frozen encoder cannot continue to
adapt its decision boundary to the genealogy structure that the
projector head already encoded, leaving the linear probe
underpowered.

\paragraph{\textsc{Tracod} (TRACO with EMA self-Distillation).} A
DINO-style EMA teacher with centered targets is layered on top of
\method. At $20\%$ CoDET-M4 the test Macro-F1 collapses to $0.403$,
roughly half its baseline. We attribute this to the momentum target
accumulating the encoder's own sibling confusion and reinforcing
it---a failure mode known from self-distillation literature when
the teacher is also confused.

\paragraph{\textsc{Geneprint} (Tri-channel S-fact disentanglement).}
A hero-attempt method that splits the projection into three
channels of $256$ dimensions each---topology, decoding, motif---and
trains them with HSIC orthogonality plus channel-specific
auxiliaries. The objective was achieved: HSIC pairwise was below
$0.003$, Spearman correlation of $z_T$ with $d_{\mathcal{T}}$ was
$+0.81$ on AICD-T2. The classifier nevertheless ignored each
channel: zero-out ablation of any single channel dropped test
Macro-F1 by at most $0.025$ on both benchmarks. The finding is
genuinely negative: \emph{author identity, on these benchmarks, is
not factorisable along the S-fact channel lines we tried}, no
matter how cleanly the channels are forced to be orthogonal.

\paragraph{Common thread.} The four failures share a structure: each
imposes additional architectural constraint that, in adjacent
problem settings, is reported to help. On code attribution with
limited data, the constraint either (i) over-regularizes the
encoder (\textsc{Tieh}, \textsc{Tracod}), (ii) under-uses the
encoder (\textsc{SetFit-Tw}), or (iii) is ignored by the
classifier head (\textsc{Geneprint}). The simplest objective in our
study---a soft re-weighting of supervised contrastive negatives by
tree distance---navigates all three failure modes by adding zero
architectural constraint and one scalar hyperparameter.

\end{document}
